domains.text is a... weird file... these are like...
these are kinda message templates for certain things added to the agents context as messages?

Agent, context, message... sessions....

state

The word "context" makes sense to me... both for "agent context" and "actor context"

I've made the actor concept do... a lot of work, in a way that makes it hard to do all of the work.

"actors" own "identity"

they are like "actors in a play"

profile...

so many super overloaded concepts....

it's always been this way though

