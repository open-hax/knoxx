I'm thinkin... it might be a better idea to save the sessions to mongo directly rather than
sending them to openplanner.


Openplanner is just the api we expose to the data.
Mongo is the real interface to that data.
