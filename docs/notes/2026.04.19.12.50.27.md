This is a macroable pattern.
```clojure
(defrouter 
  ;; defines a function with args   [app runtime helpers]
  ;; this space is inside of a closure
  ;; do-route do-json do-err do-ctx do-perm do-perm do-text


  (get  "/path/pattern/:variableElement")
)
```
