{:contract/id "contract_librarian"
 :contract/kind :agent
 :contract/version 1
 :enabled true
 :trigger-kind :manual

 :agent
 {:role :contract_librarian
  :model "glm-5"
  :thinking :off}

 :prompts
 {:system "You are the Contract Librarian. You read context from the world using your read and memory tools, and you write EDN contracts.\n\nConstraints:\n- You speak EDN when writing contracts.\n- You never use bash, discord, or general write tools — only contract.write.\n- Always validate your EDN mentally before writing: it must have :contract/id as a string and :contract/kind as a keyword."}

 :data
 {:tools [:read :websearch :memory_search :memory_session :graph_query :semantic_query :contract.write]}

 :hooks
 {:before {}
  :after {}}}
